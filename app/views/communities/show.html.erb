<div class="community-show-wrapper">
  <div class="container">
    <div class="row">
      <div class="col-md-3">
        <%= render :partial => 'users/profire', :locals => { user: @community.user } %>
      </div>
      <div class="col-md-6">
        <div class="show-container">
          <h2 class="text center"><%= @community.title %></h2>
          <div class="post-container">
            <%= form_for @post,:url => {controller: "posts", action: "create" } do |f| %>
            <div class="form-group "> 
              <%= f.text_area :content, class: 'form-control center' %>
            </div>
            <div class="form-group">
              <%= f.hidden_field :user_id, :value => current_user.id %>
            </div>
            <div class="form-group">
               <%= f.hidden_field :community_id, :value => @community.id %>
            </div>
              <%= f.submit '投稿', class: 'post-btn btn-black btn-default' %>
            <% end %>
          </div>
          <div class="post">
          　<div class="content-container">
              <% @community.posts. each do |post| %>
                <div class="post-screen bottom-border">
                  <%= link_to user_path(:id => post.user_id), class: 'user-name font-black' do %>
                    <%= image_tag post.user.image.url, class: "user-image" %>
                  <% end %>
                  <div class="user-content inline">
                    <%= link_to user_path(:id => post.user_id), class: 'user-name font-black' do %>
                      <%= post.user.name %>
                    <% end %>
                    <p class="content new-line"><%= post.content %></p>
                  </div>
                </div>
              <% end %>
            </div>
          </div>
        </div>
      </div>
     
      <div class="col-md-3">
        <div class="show-container-right">
         <%= render :partial => 'communities/admin', :locals => { community: @community } %>
         <div class="btns center">
           <% if current_user.id == @community.user_id %>
              <div class="owner center">
                <%= link_to "編集", edit_community_path(@community.id), class: 'owner-btn btn btn-default' %>
                <%= link_to "削除", community_path(@community.id), class: 'owner-btn btn btn-default', method: :delete %>
              </div>
            <% elsif !@community.join_community_users.include?(current_user) %>
              <%= link_to "参加", join_communities_path(:user_id => current_user.id, :community_id => params[:id]), class: 'join-btn btn btn-default', method: :post %>
            <% else %>
              <%= link_to "参加中", join_community_path(:user_id => current_user.id, :community_id => params[:id]), class: 'join-btn btn btn-success', method: :delete %>
            <% end %>
         </div>
          <p class="join-member title">参加メンバー</p>
          <p class="join-member-count"><%= @community.join_communities.count %>人</p>
          <div class="member">
            <% @community.join_communities. first(5). each do |community| %>
              <div class ="mamber-info bottom-border">
                <%= link_to user_path(:id => community.user_id), class: 'user-name font-black'  do %>
                  <%= image_tag community.user.image.url, class: "join-user-image" %>
                  <%= community.user.name %>
                <% end %>
              </div>
            <% end %>
            <div class="plus-member">
              <% if @community.join_communities.count > 5 %>
                <%= link_to "さらに表示", community_join_communities_path(:community_id => params[:id]), class: 'btn center' %>
              <% end %>
            </div>
          </div>
        </div>
      </div>

    </div>
  </div>
</div>

