<div class="profile ">
  <%= image_tag current_user.image.url, class: 'show-image center' %>
  <div class="user-name center">
    <%= link_to user_path(:id => current_user.id), class: 'font-black' do %>
      <%= current_user.name %>
    <% end %>
  </div>
    <div class="my-page item center">
      <%= link_to "マイページ", user_path(id: current_user.id), class: 'font-black' %>
    </div>
    
</div>